# Hunt Report — unstaged changes (6 files) — 2026-03-27T18:45:00

## Summary
FINDER_TOTAL: 11
SKEPTIC_DISPROVED: 0
ARBITER_REINSTATED: 0
VERIFIED_BUGS: 11
BREAKDOWN: 1 critical, 7 medium, 3 low

## Verified Bugs

### C1: CRITICAL — packages/hooks/secret-guard/hook.py:25-34
DESCRIPTION: _patterns.py load-failure fallback hard-codes `hookEventName: "PreToolUse"` — for a PostToolUse invocation with broken patterns, deny carries wrong event name; secrets in Bash output would not be blocked.
ORIGINAL_BUG_ID: B-008
FIX_HINT: At module load time the event is unknown; emit a generic deny without `hookEventName`, or emit both event variants, or just exit(1) to let Claude Code treat it as a hook error.

### M1: MEDIUM — packages/hooks/secret-guard/hook.py:43
DESCRIPTION: `data.get("hook_event_name", "PreToolUse")` reads snake_case key but Claude Code sends camelCase `"hookEventName"` — PostToolUse branch is silently dead code if key never matches.
ORIGINAL_BUG_ID: B-009
FIX_HINT: Verify which key Claude Code actually sends in PreToolUse/PostToolUse stdin; use the correct casing. Also check danger-detector for the same mismatch.

### M2: MEDIUM — packages/core/hook.py:67-76
DESCRIPTION: `fail_closed` decorator always calls `write_deny` with default `event_name="PreToolUse"` — if it fires in a PostToolUse handler, emitted JSON has wrong hookEventName and the deny may be ignored by the runtime.
ORIGINAL_BUG_ID: B-004
FIX_HINT: Pass the event name to `fail_closed`, or have `fail_closed` read it from the already-parsed data, or accept that a misrouted deny is still better than no deny.

### M3: MEDIUM — packages/hooks/danger-detector/hook.py:56-65
DESCRIPTION: Allow-list check uses `if pattern in command` (substring) — a short entry like `"rm"` would suppress `rm-recursive` detection for any command containing `"rm"` (e.g. `chmod`, `firmware update`).
ORIGINAL_BUG_ID: B-005
FIX_HINT: Document that allow-patterns must be specific substrings (not single tokens), or switch to exact-prefix matching; add a validation warning if an allow-pattern is fewer than 8 chars.

### M4: MEDIUM — packages/core/bypass.py:79-80
DESCRIPTION: Truncation guard `if len(raw) == 8192: lines = lines[1:]` also fires when the file is exactly 8192 bytes and fully read — valid first line is discarded, losing a real bypass signal.
ORIGINAL_BUG_ID: B-002
FIX_HINT: Store the file size before slicing: `file_size = path.stat().st_size; raw = path.read_bytes()[-8192:]; if file_size > 8192: lines = lines[1:]`

### M5: MEDIUM — packages/hooks/danger-detector/hook.py:30-36
DESCRIPTION: `unwrap` does not handle `$(...)` or backtick substitution — dangerous payloads embedded in `$(rm -rf /)` or `` `rm -rf /` `` forms bypass detection entirely.
ORIGINAL_BUG_ID: B-006
FIX_HINT: Add regex branches for `\$\(([^)]+)\)` and `` `([^`]+)` `` to `unwrap`, or apply detection to both the original command and all extracted subcommands recursively.

### M6: MEDIUM — packages/hooks/danger-detector/hook.py:25
DESCRIPTION: Fork-bomb regex `[^}]*` stops at first `}` — variants with nested braces like `:() { { :|: ; } }` evade the pattern.
ORIGINAL_BUG_ID: B-007
FIX_HINT: Use `[^;]*` instead of `[^}]*` to stop at statement terminator rather than brace, or use a lookahead: `:\(\).*\{.*:\|.*:`.

### M7: MEDIUM — packages/core/bypass.py:74-76
DESCRIPTION: TOCTOU — `path.exists()` check followed by `path.read_bytes()` is not atomic; file can be deleted between the two calls causing an OSError caught silently.
ORIGINAL_BUG_ID: B-001
FIX_HINT: Remove the `path.exists()` guard entirely; let `read_bytes()` raise `FileNotFoundError` which is caught by the outer `(PermissionError, OSError)` handler — one syscall, no race.

### L1: LOW — packages/core/bypass.py:107
DESCRIPTION: Unreachable `return False` at end of function — all prior paths return or raise; dead code.
ORIGINAL_BUG_ID: B-003
FIX_HINT: Remove the dead `return False` statement.

### L2: LOW — packages/hooks/danger-detector/hook.py:12
DESCRIPTION: `sys.path.insert(0, str(Path(__file__).parent.parent.parent.parent))` — four `.parent` levels is a fragile deployment assumption; breaks if file is symlinked or installed at a different depth.
ORIGINAL_BUG_ID: B-010
FIX_HINT: Use `CLAUDE_PLUGIN_ROOT` env var (already used in adapter entrypoints) as the authoritative root, falling back to the four-parent heuristic.

### L3: LOW — packages/core/bypass.py:99-100
DESCRIPTION: Content ordering assumption — code expects `content[0].type == "text"` for user turns; if Claude Code ever places a `tool_result` at position 0, function scans backwards and may find a stale `"* "` from a prior turn.
ORIGINAL_BUG_ID: B-011
FIX_HINT: Scan ALL content blocks in the entry (not just index 0) for a `type=text` block; don't assume position.

## False Positives (Skeptic dismissed, Arbiter confirmed)
None — Skeptic accepted all 11 bugs (0 disproved).
